language: node_js
node_js:
- "6"
services:
  - postgresql: "9.6"
before_script:
  - psql -c "create database dms-test;" -U postgres
  - psql -c "CREATE USER Niccie;" -U postgres
  - sleep 3
after_success:
  - npm run coverage
env:
  - SECRET_TOKEN_KEY=nottheactualthing NODE_ENV=test
